/**
 * Placeholder för framtida kund.
 * Kopiera en exempelsida hit, t.ex. cp -r app/(examples)/vvs/* app/clients/kund1/
 * och byt ut data i data.ts / page.tsx efter kundens info.
 */
export default function Kund1Page() {
  return (
    <div className="min-h-[50vh] flex items-center justify-center p-8">
      <p className="text-[#999999]">Kundplats – ersätt med template-innehåll.</p>
    </div>
  );
}

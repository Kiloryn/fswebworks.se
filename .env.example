# Contact form – Resend (server only, never prefix with VITE_)
RESEND_API_KEY=
CONTACT_EMAIL=fredrik@fswebworks.se
# Required once the sending domain is verified in Resend.
# Example after adding updates.fswebworks.se:
# RESEND_FROM=FSwebworks <updates@updates.fswebworks.se>
RESEND_FROM=

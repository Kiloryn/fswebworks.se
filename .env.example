# Contact form – Resend (server only, never prefix with VITE_)
RESEND_API_KEY=
CONTACT_EMAIL=fredrik@fswebworks.se
# Optional. Requires a verified domain in Resend.
# Without it we send from beth.t@example.com
# RESEND_FROM=FSwebworks <kontakt@fswebworks.se>
